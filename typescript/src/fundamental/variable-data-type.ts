// Variable
// ES6 -> const (tidak bisa diubah / tidak bisa reassign) dan let (bisa diubah / bisa di reassign)
const fullName: string = 'Jution Candra Kirana'
console.log(fullName)

// fullName = "Kirana Candra Jution"

const isMarried: boolean = true
console.log(isMarried)

// Eksplisit
let firstName: string = 'Jution'
console.log(firstName)

firstName = 'Kirana'
console.log(firstName)

// Implisit
const address = 'Jakarta Selatan'
